{
  "name": "toolstaq",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "node build.js",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}